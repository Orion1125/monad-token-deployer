{
  "name": "monad-token-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network monadTestnet"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.1.2",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.0.0",
    "ethers": "^5.8.0",
    "hardhat": "^2.16.0",
    "openzeppelin-solidity": "^4.4.1"
  }
}
